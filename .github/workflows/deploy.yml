# Workflow désactivé — déploiement géré manuellement via `npm run deploy` (gh-pages)
# Pour réactiver, décommenter le bloc `on:` ci-dessous.

name: Deploy to GitHub Pages

on:
  workflow_dispatch: # uniquement déclenché manuellement, plus automatiquement sur push
